package com.hadeso.moviedb.architecture.base

/**
 * An [Action] is the only way to trigger a [State]
 * mutation in the [Store]. It also carries the payload
 * needed to generate the new [State].
 */
interface Action
